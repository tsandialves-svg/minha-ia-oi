export async function createPreapproval(plan:'monthly'|'yearly', email:string) { const response=await fetch('https://api.mercadopago.com/preapproval',{method:'POST',headers:{Authorization:`Bearer ${process.env.MERCADO_PAGO_ACCESS_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({reason:`Norte IA - Plano ${plan}`,payer_email:email,external_reference:email,back_url:`${process.env.NEXT_PUBLIC_APP_URL}/conta`,auto_recurring:{frequency:plan==='monthly'?1:12,frequency_type:'months',transaction_amount:plan==='monthly'?100:1000,currency_id:'BRL'}})}); if(!response.ok)throw new Error('Mercado Pago indisponível'); return response.json(); }
