'use client';
import { FormEvent,useState } from 'react';
import { browserSupabase } from '@/lib/supabase';
export default function AuthForm({mode}:{mode:'login'|'signup'}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(event:FormEvent){event.preventDefault();setLoading(true);setError('');const supabase=browserSupabase();const result=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});if(result.error)setError('Não foi possível concluir. Confira seus dados e tente novamente.');else window.location.href=mode==='login'?'/chat':'/login?confirmado=1';setLoading(false)}return <form onSubmit={submit} className="mt-9 space-y-5"><label className="block text-sm font-bold">E-mail<input required type="email" value={email} onChange={event=>setEmail(event.target.value)} className="mt-2 w-full rounded-xl border border-ink/20 bg-transparent px-4 py-3 outline-none focus:border-moss" placeholder="voce@email.com"/></label><label className="block text-sm font-bold">Senha<input required minLength={8} type="password" value={password} onChange={event=>setPassword(event.target.value)} className="mt-2 w-full rounded-xl border border-ink/20 bg-transparent px-4 py-3 outline-none focus:border-moss" placeholder="Mínimo de 8 caracteres"/></label>{error&&<p className="text-sm text-coral">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-ink py-4 font-bold text-mist disabled:opacity-50">{loading?'Aguarde...':mode==='login'?'Entrar':'Criar minha conta'}</button></form>}
